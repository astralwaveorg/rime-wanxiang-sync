# GitHub Actions: 自动同步 Rime 万象方案并集成个人配置

name: 自动同步 Rime 万象方案

on:
  schedule:
    # 每隔一天在 UTC 19:20 (北京时间 3:20) 运行
    - cron: '20 19 */2 * *'

  workflow_dispatch: # 允许手动触发

jobs:
  sync-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 赋予提交代码和创建 Release 的权限

    steps:
      - name: 🚚 检出代码
        uses: actions/checkout@v4

      - name: 🔍 查找最新有效版本
        id: get_release
        run: |
          echo "🔄 正在查找 amzxyz/rime_wanxiang 的最新有效版本..."
          API_URL="https://api.github.com/repos/amzxyz/rime_wanxiang/releases"
          RELEASES_INFO=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" "$API_URL?per_page=30")
          # 筛选出资产中包含 "rime-wanxiang-base.zip" 的最新 Release
          TARGET_RELEASE=$(echo "$RELEASES_INFO" | jq -c '[.[] | select(.assets | map(.name) | contains(["rime-wanxiang-base.zip"]))][0]')

          if [ -z "$TARGET_RELEASE" ] || [ "$TARGET_RELEASE" == "null" ]; then
            echo "❗️ 错误：未找到包含 'rime-wanxiang-base.zip' 的有效版本。"
            exit 1
          fi

          RELEASE_VERSION=$(echo "$TARGET_RELEASE" | jq -r .tag_name)
          DOWNLOAD_URL=$(echo "$TARGET_RELEASE" | jq -r '.assets[] | select(.name == "rime-wanxiang-base.zip") | .browser_download_url')

          echo "✅ 成功找到目标版本: ${RELEASE_VERSION}"
          echo "release_version=${RELEASE_VERSION}" >> $GITHUB_OUTPUT
          echo "download_url=${DOWNLOAD_URL}" >> $GITHUB_OUTPUT

      - name: 📥 下载并解压上游方案
        run: |
          # 使用一个固定的临时目录路径
          TEMP_DIR="/tmp/rime_sync"
          mkdir -p ${TEMP_DIR}/source

          echo "⏳ 正在下载 Release 文件..."
          curl -L "${{ steps.get_release.outputs.download_url }}" -o "${TEMP_DIR}/rime-wanxiang-base.zip"

          echo "📦 正在解压文件..."
          unzip -qo "${TEMP_DIR}/rime-wanxiang-base.zip" -d "${TEMP_DIR}/source/"
          echo "✅ 解压成功。"

      - name: 🔄 同步上游文件
        run: |
          # 直接引用上一步中使用的相同临时目录路径
          SOURCE_DIR="/tmp/rime_sync/source"

          echo "⏳ 正在同步文件至仓库..."
          rsync -av --delete \
            --exclude '.git/' \
            --exclude '.github/' \
            --exclude 'custom/' \
            --exclude 'custom_phrase.txt' \
            --exclude 'README.md' \
            --exclude '.gitignore' \
            "${SOURCE_DIR}/" "./"
          echo "✅ 上游文件同步完成。"

      - name: 📂 复制个人配置
        run: |
          echo "📂 正在从 'custom' 目录完整复制个人配置..."
          if [ -d "custom" ]; then
            # 使用 -a (archive) 模式递归复制所有内容（文件、子目录、软链接等）
            # `custom/.` 表示复制 custom 目录的 *内容*，而非目录本身
            cp -a custom/. ./
            echo "✅ 个人配置复制完成。"
          else
            echo "⚠️ 'custom' 目录不存在，跳过复制。"
          fi

      - name: ✨ 提交并发布新版本
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # 检查是否有文件变更
          if [[ -z $(git status --porcelain) ]]; then
            echo "👍 文件已是最新，无需操作。"
            exit 0
          fi

          echo "📝 检测到变更，正在提交..."
          git add -A
          COMMIT_MSG="chore: 同步 amzxyz/rime_wanxiang v${{ steps.get_release.outputs.release_version }}"
          git commit -m "${COMMIT_MSG}"

          echo "🚀 正在推送至 main 分支..."
          git push --force

          NEW_TAG=$(date +v%y.%m.%d)
          echo "🏷️  生成新标签: ${NEW_TAG}"

          echo "📦 正在打包 Release (已过滤指定文件)..."
          # 打包时，排除 .git, custom 目录, 以及指定的几个配置文件
          zip -qr rime.zip . -x ".git/*" -x "custom/*" -x "README.md" -x ".gitignore" -x "CHANGELOG.md"

          echo "🚀 正在创建 GitHub Release..."
          gh release create "$NEW_TAG" \
            ./rime.zip \
            --title "自动同步发行版 ${NEW_TAG}" \
            --notes "本次更新自动同步自 amzxyz/rime_wanxiang 的版本: **${{ steps.get_release.outputs.release_version }}**"

          echo "✅ 新发行版 ${NEW_TAG} 创建成功！"
